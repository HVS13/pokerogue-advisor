{
  "name": "pokerogue-advisor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:types && npm run build:extension",
    "build:types": "tsc -p tsconfig.json",
    "build:extension": "node scripts/build-extension.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "test:core": "npm run build:types && node tests/capture-decision.mjs && node tests/catch-value.mjs && node tests/battle-decision.mjs && node tests/reward-shop.mjs && node tests/browser-flow.mjs",
    "test:bridge": "tsc -p tests/bridge/tsconfig.json",
    "test:official-bridge": "tsc -p tests/official-bridge/tsconfig.json",
    "test:installer": "node tests/install-2p.mjs && node tests/install-official.mjs",
    "test": "npm run check && npm run test:core && npm run test:bridge && npm run test:official-bridge && npm run test:installer && npm run test:bundle",
    "test:bundle": "npm run build:extension && node --check extension/advisor.js && node tests/bundle-smoke.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
